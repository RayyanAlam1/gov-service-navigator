## What this changes

<!-- Why the change is needed. The diff already shows what changed. -->

## How it was verified

<!-- Commands you ran and what they reported. -->

- [ ] `npm run verify` (typecheck, tests, evaluation suite)
- [ ] `npm run lint`

## Grounding

Every change that can put text on screen has to answer this.

- [ ] No government fact in this change originates from model output
- [ ] `npm run eval` still reports **0 unsupported claims**
- [ ] Any new fact carries a provenance tier and, if `verified`, a citable source

If this change does not touch the answer path, say so and tick the boxes.

## Screenshots

<!-- For interface changes. Include Urdu or Roman Urdu if the change affects
     layout, because RTL and Naskh break differently from Latin. -->
